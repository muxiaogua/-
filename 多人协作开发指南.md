# Jason Team「团队工作台」多人协同开发与协作指南

欢迎加入团队工作台的开发！本项目基于 **SwiftUI + macOS Native** 原生架构开发。为了保证多人并行开发互不冲突，请遵循以下开发协作标准流程。

---

## 一、 核心架构与任务分工表

| 任务板块 | 归属一级菜单 | 认领负责人 | 目标文件路径（新建） | 对应路由开关 |
| :--- | :--- | :--- | :--- | :--- |
| **价格查询** | 查询中心 | 开发者 A | `Views/PriceQueryView.swift` | `ContentView.swift` 中替换 `.priceQuery` |
| **我要请假** | 个人中心 | 开发者 B | `Views/LeaveRequestView.swift` | `ContentView.swift` 中替换 `.leaveRequest` |
| **数据统计** | 个人中心 | 开发者 B | `Views/MyStatsView.swift` | `ContentView.swift` 中替换 `.myStats` |
| **案例协助** | 互帮互助 | 开发者 C | `Views/CaseAssistanceView.swift` | `ContentView.swift` 中替换 `.caseAssistance` |
| **共享知识库** | 互帮互助 | 开发者 C | `Views/SharedKnowledgeView.swift` | `ContentView.swift` 中替换 `.sharedKnowledge` |
| **幸运大转盘** | 小工具 | 开发者 D | `Views/LuckyWheelView.swift` | `ContentView.swift` 中替换 `.luckyWheel` |
| **日期计算器** | 小工具 | 开发者 D | `Views/DateCalculatorView.swift` | `ContentView.swift` 中替换 `.dateCalculator` |

---

## 二、 协作者（其他组员）如何参与开发？

### 第 1 步：克隆仓库到本地
在终端中执行：
```bash
git clone <仓库地址>
cd 团队工作台
```

### 第 2 步：创建自己的独立特性分支（禁止直接在 main 分支改动）
例如开发者 A 负责「价格查询」：
```bash
git checkout -b feature/price-query
```

### 第 3 步：开始写代码
1. 在 `团队工作台/Views/` 目录下新建自己的 View 文件（如 `PriceQueryView.swift`）；
2. 在 `团队工作台/ContentView.swift` 找到 `mainContentArea`，将对应的 `PlaceholderReservedView` 替换为您的 View：
   ```swift
   case .priceQuery:
       PriceQueryView()
   ```
3. 在 Xcode 中按 `Cmd + R` 运行调试。

### 第 4 步：提交并推送到远程
```bash
git add .
git commit -m "完成价格查询基础界面与报价检索逻辑"
git push -u origin feature/price-query
```
然后在 GitHub / GitLab 网页上点击 **「Create Pull Request (创建 PR)」** 提交给负责人审核。

---

## 三、 主负责人（您）如何查看、测试与合并？

### 第 1 步：查看组员提交的内容
- 打开 GitHub / GitLab 仓库页面，点击 **「Pull Requests」** 标签页；
- 您能清楚看到每位组员提交的代码改动（增加了哪些文件、修改了哪几行）。

### 第 2 步：在本地拉取组员分支进行测试
如果您想在本地 Xcode 亲自运行测试组员的代码：
```bash
git fetch origin
git checkout feature/price-query
```
在 Xcode 中按 `Cmd + R` 运行体验。

### 第 3 步：一键合并到主干
- 如果测试通过，直接在 GitHub / GitLab 网页的 PR 页面点击 **「Merge Pull Request (合并 PR)」**；
- 或者在本地主干合并：
  ```bash
  git checkout main
  git pull origin main
  git merge feature/price-query
  git push origin main
  ```

---

## 四、 如何打包正式应用分发给全员使用？

当功能合并完成后，生成免安装的 `.app` 应用程序发送给全员：
1. 打开 Xcode，顶部选择目标为 **`My Mac`**；
2. 菜单栏点击 **`Product > Archive`**；
3. 打包完成后在弹出的 Organizer 窗口中，点击 **`Distribute App`** -> 选择 **`Direct Distribution`** 或 **`Export`**；
4. 导出后会得到 `团队工作台.app`，将其压缩成 zip 发送给组员，大家双击即可直接使用！
